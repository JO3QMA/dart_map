# 次により legacy-peer-deps を有効化している:
# - react-leaflet v5 は React 19 を peer に指定するため本件では不要になったが、
# - @tailwindcss/vite@4.x は peer に vite@^5.2.0||^6||^7 のみ指定しており vite@8 を含まないため。
# CI およびローカルで npm install / npm ci が通るよう legacy-peer-deps を有効化する。
legacy-peer-deps=true
